using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GithubOrnek : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Github Örnek");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
